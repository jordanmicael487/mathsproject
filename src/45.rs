// This is a simple example of a Rust program.
fn main() {
    // Define some variables
    let x = 10;
    let y = 5;

    // Perform arithmetic operations on the variables
    println!("The result of {} + {} is {}", x, y, x.add(y));
}
